apiVersion: v1
kind: Service
metadata:
  name: {{ include "castle-bridge-client.fullname" . }}
  labels:
    {{- include "castle-bridge-client.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "castle-bridge-client.selectorLabels" . | nindent 4 }}
